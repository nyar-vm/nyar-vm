# @nyar-vm/nyar-win32-x64

Windows x64 native binary distribution for the Nyar virtual machine.

## Overview

This is a platform-specific package containing the native `nyar` binary for Windows x64. It is intended to be used as an optional dependency of the main `@nyar/nyar` package.

## License

MIT
