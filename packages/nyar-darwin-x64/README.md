# @nyar-vm/nyar-darwin-x64

macOS x64 native binary distribution for the Nyar VM.

## Overview

This is a platform-specific package containing the native `nyar` binary for macOS x64. It is intended to be used as an optional dependency of the main `@nyar/nyar` package.

## License

MIT
