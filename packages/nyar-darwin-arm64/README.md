# @nyar-vm/nyar-darwin-arm64

macOS ARM64 native binary distribution for the Nyar VM.

## Overview

This is a platform-specific package containing the native `nyar` binary for macOS ARM64. It is intended to be used as an optional dependency of the main `@nyar/nyar` package.

## License

MIT
