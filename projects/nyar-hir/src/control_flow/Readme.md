


## For statement

```markdown
for `pattern` in `iterator` {
    `[body]`
}
else {
    `[otherwise]`
}
```

## Loop statement

```markdown
@head
loop {
    @begin
    @body
    @end
}
@tail
```

## While statement

```markdown
while `condition` {
    `[body]`
}
else {
    `[otherwise]`
}
```