[package]
name = "valkyrie-minimal"
version = "0.1.0"
edition = "2021"

[lib]
name = "valkyrie_minimal"
path = "src/lib.rs"

[dependencies]
nyar-vm = { path = "../nyar-vm" }
toml = { workspace = true }

