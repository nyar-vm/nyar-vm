[package]
name = "nyar-gc"
version = "0.1.0"
edition = "2021"

[dependencies]
nyar-macros = { workspace = true }
tokio = { version = "1.0", features = ["full"], optional = true }
once_cell = "1.18"
dashmap = "5.5"
parking_lot = "0.12"

[features]
default = []
tokio = ["dep:tokio"]
