[package]
name = "nyar-error"
version = "0.1.0"
authors = ["192607617@qq.com"]
edition = "2021"
description = "..."
repository = "https://github.com/nyar-lang/NyarNative"
license = "MPL-2.0"

[dependencies]
serde = {version = "1.0.143", features = ["derive"]}
num = {version="0.4.0", features = ["serde"], optional = true}
bigdecimal = {version="0.3.0", features = ["serde"], optional = true}

pest = {version = "2.2.1", optional = true}


[dev-dependencies]

[features]
default = ["num", "bigdecimal"]
