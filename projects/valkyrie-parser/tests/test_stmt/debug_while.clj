(block scoped
    while)
(block scoped
    (block scoped
        (block scoped
            looping)))
(block scoped
    while)
(block scoped
    true)
(block scoped
    (block scoped
        (block scoped
            looping)))
(block scoped
    while)
(block scoped
    a)
(block scoped
    (block scoped
        (block scoped
            (switch
                (case
                    a)
                (then
                    (block scoped
                        break))
                (else
                    (block scoped
                        continue))))))
(block scoped
    else)
(block scoped
    (block scoped
        (block scoped
            nothing)))
(block scoped
    while)