class Point { x, y }
let p = new Point()
p.x = 10
p.y = 20
print(p.x)
print(p.y)

micro sum(obj) {
    return obj.x + obj.y
}

print(sum(p))

let check = p is Point
print(check)

let p2 = p as Point
print(p2.x)

# class Another { a }
# let p3 = p as Another
