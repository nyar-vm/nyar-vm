function add(a: number, b: number): number {
    if (a > 0) {
        return a + b;
    } else {
        return b;
    }
}

let x = 10;
let y = 20;
let z = add(x, y);

let i = 0;
while (i < 10) {
    i = i + 1;
}

z;
