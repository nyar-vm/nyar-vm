using package::lexer::Lexer
using package::parser::Parser

let source = "let x = 10 + 20; # comment\n print(x);"
let l = Lexer::new(source)
let p = Parser::new(l)
let program = p.parse_all()
print(program)
