namespace package::backend::jvm;

using package::lir

class JVMBackend { }

imply JVMBackend {
  micro emit(self, lir) {
    let in_path = "examples/valkyrie-bootstrap/target/vcc_new.nyarc"
    let out_path = self.output_path()
    return out_path
  }
  micro output_path(self) {
    return "examples/valkyrie-bootstrap/target/vcc_new.jar"
  }
}
